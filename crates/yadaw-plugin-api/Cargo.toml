[package]
name = "yadaw-plugin-api"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0"
description = "Plugin API definitions for YADAW audio plugins"

[dependencies]
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }