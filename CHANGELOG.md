## v0.10.7

- No user-facing changes were mentioned since previous release


## v0.10.6

- No user-facing changes were mentioned since previous release


## v0.10.5

- No user-facing changes were mentioned since previous release


## v0.10.4

- bump to android api 37


## v0.10.3

- fix export being shrinked
- empty area right click menu for paste
- track duplication
- web: fix
- compile errs
- wasm: fix web threads, and bump other vers
- fix wasm
- wasm-safe-* crates replaced by web-workers, and plugin workers refactor
- for nih_plug uis
- bump egui to 0.36
- revert: build-std handled by rust-toolchain.toml
- feat: enable build-std
- refactor: use reusable pages workflow
- bump ver and cargo update (for plugin hosts)
- add adaptive icons for android
- update comment
- fix the lv2 reopen crash
- lv2 basic UI display
- fix move plugin down button
- web: gating for x11
- web: more switching
- Replace SafeMutex and flume channels with wasm_safe_mutex variants for the UI
- add service worker for gh pages coep
- auto save cfg files on every change
- web: audio caching cont (not ready yet)
- web: audio caching init via opfs
- rm the gatings for wasm export
- fix example
- web: wire export (test)
- pin to nightly for rayon
- fix: the plugin is added to the track for which + was clicked (even when not focused)
- add provides for clap and lv2 to aur pkgbuild
- snap icon


# Changelog

## v0.10.2

- fix export being shrinked
- empty area right click menu for paste
- track duplication
- web: fix
- compile errs
- wasm: fix web threads, and bump other vers
- fix wasm
- wasm-safe-* crates replaced by web-workers, and plugin workers refactor
- for nih_plug uis
- bump egui to 0.36
- revert: build-std handled by rust-toolchain.toml
- feat: enable build-std
- refactor: use reusable pages workflow
- bump ver and cargo update (for plugin hosts)
- add adaptive icons for android
- update comment
- fix the lv2 reopen crash
- lv2 basic UI display
- fix move plugin down button
- web: gating for x11
- web: more switching
- Replace SafeMutex and flume channels with wasm_safe_mutex variants for the UI
- add service worker for gh pages coep
- auto save cfg files on every change
- web: audio caching cont (not ready yet)
- web: audio caching init via opfs
- rm the gatings for wasm export
- fix example
- web: wire export (test)
- pin to nightly for rayon
- fix: the plugin is added to the track for which + was clicked (even when not focused)
- add provides for clap and lv2 to aur pkgbuild
- snap icon

